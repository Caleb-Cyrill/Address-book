<% if @person.errors.any? %>
    alert(@person.errors.count);
<% else %>
    $("#peopleTable").append("<%= escape_javascript render( 'person_row', person: @person) %>");
    $(".close-modal").click();
<% end %>