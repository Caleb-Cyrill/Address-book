<% if @phone_number.errors.any? %>
    alert('Invalid phone number details');
<% else %>
    $("#phoneRow_<%= @phone_number.id %>").replaceWith("<%= escape_javascript render( 'phone_row', phone_number: @phone_number) %>");
    $(".close-modal").click();
<% end %>