</br>
    <div class="card text-center">
        <div class="card-header">
          <ul class="nav nav-tabs card-header-tabs" id="myTab">
            <li class="nav-item">
              <a href="#home" class="nav-link active" data-bs-toggle="tab">Contact Info</a>
            </li>
            <li class="nav-item">
              <a href="#Addresses" class="nav-link" data-bs-toggle="tab">Addresses</a>
            </li>
            <li class="nav-item">
              <a href="#Emails" class="nav-link" data-bs-toggle="tab">Emails</a>
            </li>
            <li class="nav-item">
              <a href="#PhoneNumbers" class="nav-link" data-bs-toggle="tab">Phone Numbers</a>
            </li>
          </ul>
        </div>

        <div class="card-body">
            <div class="tab-content">
                <div class="tab-pane fade show active" id="home">
                    <h5 class="card-title"><strong><%= @person.salutations %> <%= @person.first_name %> <%= @person.middle_name %> <%= @person.last_name %></strong></h5>
                      <p class="card-text"></p>
                </div>
                <div class="tab-pane fade" id="Addresses">
                  <div class=table-responsive>
                    <table class="table table-striped table-bordered table-hover">
                      <thead class="table-dark">
                        <tr>
                          <th>Street</th>
                          <th>Town</th>
                          <th>Zip code</th>
                          <th>State</th>
                          <th>Country</th>
                          <th colspan="1"></th>
                        </tr>
                      </thead>

                      <tbody>
                        <% @address.each do |address| %>
                          <tr>
                            <td><%= address.street %></td>
                            <td><%= address.town %></td>
                            <td><%= address.zip_code %></td>
                            <td><%= address.state %></td>
                            <td><%= address.country %></td>
                            <td><%= link_to 'Edit', edit_person_address_path(@address), class:"btn btn-primary"%></td>
                          </tr>
                        <% end %>
                      </tbody>
                    </table>
                  </div>
                </div>
                <div class="tab-pane fade" id="Emails">
                    <div class=table-responsive>
                      <table class="table table-striped table-bordered table-hover">
                        <thead class="table-dark">
                          <tr>
                            <th>Email</th>
                            <th>Comment</th>
                            <th colspan="1"></th>
                          </tr>
                        </thead>
                        <tbody>
                          <% @email.each do |email| %>
                            <tr>
                              <td><%= email.email %></td>
                              <td><%= email.comments %></td>
                              <td><%= link_to 'Edit', edit_person_address_path(email), class:"btn btn-primary"%></td>
                            </tr>
                          <% end %>
                        </tbody>
                      </table>
                    </div>
                </div>

                <div class="tab-pane fade" id="PhoneNumbers">
                    <div class=table-responsive>
                      <table class="table table-striped table-bordered table-hover">
                        <thead class="table-dark">
                          <tr>
                            <th>Phone Number</th>
                            <th>Comment</th>
                            <th colspan="1"></th>
                          </tr>
                        </thead>

                        <tbody>
                          <% @phone_number.each do |phone_number| %>
                          <tr>
                            <td><%= phone_number.phone_number %></td>
                            <td><%= phone_number.comments %></td>
                            <td><%= link_to 'Edit', edit_person_phone_number_path(@phone_number), class:"btn btn-primary"%></td>
                          </tr>
                          <% end %>
                        </tbody>
                      </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

<div class="container">
  <div class="actions">
  </br>
    <%= link_to 'Edit', edit_person_path(@person), class:"btn btn-primary" %> 
    <%= link_to 'Back to People', people_path, class:"btn btn-primary" %>
  </div>
</div>