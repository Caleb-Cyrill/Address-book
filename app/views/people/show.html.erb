</br>
    <div class="card">
        <div class="card-header">
          <ul class="nav nav-tabs card-header-tabs" id="myTab">
            <li class="nav-item">
              <a href="#home" class="nav-link active" data-bs-toggle="tab">Contact Info</a>
            </li>
            <li class="nav-item">
              <a href="#Addresses" class="nav-link" data-bs-toggle="tab">Addresses</a>
            </li>
            <li class="nav-item">
              <a href="#Emails" class="nav-link" data-bs-toggle="tab">Emails</a>
            </li>
            <li class="nav-item">
              <a href="#PhoneNumbers" class="nav-link" data-bs-toggle="tab">Phone Numbers</a>
            </li>
          </ul>
        </div>

        <div class="card-body">
            <div class="tab-content">
                <div class="tab-pane fade show active" id="home">
                    <h5 class="card-title"><strong><%= @person.salutations %> <%= @person.first_name %> <%= @person.middle_name %> <%= @person.last_name %></strong></h5>
                    <table class="table table-striped table-bordered table-hover">
                      <thead class="table-dark">
                        <tr>
                          <th>SSN</th>
                          <th>Birth date</th>
                          <th>Comment</th>
                        </tr>
                      </thead>
                      <tr>
                        <td class="card-text"><%= @person.ssn %></td>
                        <td class="card-text"><%= @person.birth_date.strftime("%m/%d/%Y") %></td>
                        <td class="card-text"><%= @person.comment %></td>
                      </tr>
                    </table>
                </div>
                <%= render 'people/addresses/addressCard' %>
                <%= render 'people/emails/emailCard' %>
                <%= render 'people/phone_numbers/phoneCard' %>
            </div>
        </div>
    </div>

<div class="container">
  <div class="actions">
  </br>
    <%= link_to 'Back to People', people_path, class:"btn btn-primary" %>
  </div>
</div>

