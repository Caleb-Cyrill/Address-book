</br>
<div class=table-responsive>
  <table class="table table-striped table-bordered table-hover">
    <thead class="table-dark">
      <tr>
        <th>Email</th>
        <th>Comment</th>
      </tr>
    </thead>

    <tbody>
      <tr>
        <td><%= @email.email %></td>
        <td><%= @email.comments %></td>
      </tr>
    </tbody>
  </table>
</div>

<%= link_to 'Edit',  edit_person_email_path([@person, @email]), class:"btn btn-primary" %> 
<%= link_to 'Back to People', people_path, class:"btn btn-primary" %> 
<%= link_to 'Back to Emails', person_emails_path, class:"btn btn-primary" %> 
